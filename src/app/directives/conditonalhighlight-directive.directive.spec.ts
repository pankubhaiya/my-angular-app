import { ConditonalhighlightDirectiveDirective } from './conditonalhighlight-directive.directive';

describe('ConditonalhighlightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditonalhighlightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
